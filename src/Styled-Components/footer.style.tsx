import Styled from 'styled-components';


export const FooterStyled = Styled.footer`
    background: black;
    height: 200px;
`;

export const ContainerFotterStyled = Styled.div`
    width: 30%;
    margin: auto;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
`;